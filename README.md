# ascendo-health

Five-minute scheduler for the Ascendo CRM's `#health` channel. It calls
`https://ascendoacademy.com/api/health?post=1`; the app does the checking and
posting. See `docs/discord.md` in `Ascendoacademy/cp-tutor`.
